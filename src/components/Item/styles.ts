import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container:{
        marginTop: 8,
        flexDirection: "row",
        alignItems: "center",
        gap: 7,
    },
    description:{
        flex: 1,
        fontSize: 12,
        fontWeight: 600
    }

})